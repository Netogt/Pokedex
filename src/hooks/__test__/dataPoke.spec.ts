import { dataPoke, dataListPoke } from "../useFetch";

const res1 = { 
    height: 6, 
    weight: 85, 
    abilities: "blaze", 
    id:, 
    name:, 
    sprites:, 
    types:, 
    stats: 
}
const res2 = { shape, generation }

describe("dataPoke", () => {
    it("testing return of the function", () => {
        const respose = [res1, res2]
    })
})